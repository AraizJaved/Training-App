import { ParticipantStatusDto } from './participant-status-dto';

describe('ParticipantStatusDto', () => {
  it('should create an instance', () => {
    expect(new ParticipantStatusDto()).toBeTruthy();
  });
});
